fn main() {
    let x: i32 = 17;
    let y: i32 = 4;
    let sum: i32 = x + y;
    let diff: i32 = x - y;
    let prod: i32 = x * y;
    let quot: i32 = x / y;
    let rem: i32 = x % y;
    printlnInt(sum);
    printlnInt(diff);
    printlnInt(prod);
    printlnInt(quot);
    printlnInt(rem);
}
